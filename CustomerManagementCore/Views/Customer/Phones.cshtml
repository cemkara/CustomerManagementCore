@model List<EntityLayer.Concrete.Phone>
@{
    ViewData["Title"] = "Customer Phones";
}

<div class="card my-4">
    <div class="card-header pb-0 p-3">
        <div class="row">
            <div class="col-6 d-flex align-items-center">
                <h6 class="mb-0">Customer Phones</h6>
            </div>
            <div class="col-6 text-end">
                <a href="/Customer/PhoneAdd/@ViewBag.customerId" class="btn btn-sm btn-outline-success mb-0">New</a>
            </div>
        </div>
    </div>

    <div class="card-body px-0 pb-2">
        <div class="table-responsive p-0">
            <table class="table align-items-center mb-0">
                <thead>
                    <tr>
                        <th class="text-uppercase text-secondary text-xxs font-weight-bolder opacity-7 ps-2">Phone</th>
                        <th class="text-uppercase text-secondary text-xxs font-weight-bolder opacity-7 ps-2">Status</th>
                        <th class="text-uppercase text-secondary text-xxs font-weight-bolder opacity-7 ps-2">Actions</th>
                    </tr>
                </thead>
                <tbody>
                    @foreach (var item in Model)
                    {
                        <tr>
                            <td>
                                @item.Number
                            </td>
                            <td>
                                @item.Status
                            </td>
                            <td class="align-middle">
                                @{
                                    string btnStatusText = "";
                                    string btnStatusClass = "";
                                    if (item.Status)
                                    {
                                        btnStatusText = "Disable";
                                        btnStatusClass = "danger";
                                    }
                                    else
                                    {
                                        btnStatusText = "Activate";
                                        btnStatusClass = "success";
                                    }
                                }
                                <a href="/Customer/PhoneStatusChange/@item.PhoneId" class="btn btn-sm btn-outline-@btnStatusClass">@btnStatusText</a>
                            </td>
                        </tr>
                    }
                </tbody>
            </table>
        </div>
    </div>
</div>

